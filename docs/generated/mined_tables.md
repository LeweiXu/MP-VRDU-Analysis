# Mined tables — candidates

Generated by `ops/mine.py`. Each surviving candidate answers a deployment question a reader of an MP-VRDU paper would ask; the user selects which graduate to the paper. Contaminated axes (decode latency, tokens/answer) are excluded by design.

## Built

| table | question | n (cells) | caveat |
| --- | --- | --- | --- |
| mined_scan_vs_digital | Does the cheap T rung collapse on scanned docs, shifting the frontier to TLV/V? | 3143 | on-thesis deployment finding; scanned T is empty by design. |
| mined_prefill_cost | What does each representation cost to ingest, on the clean (decode-free) axis? | 3143 | prefill + input tokens only; uncontaminated by the verbose-answer inflation. |
| mined_vram_headroom | How close does each spec/rung/resolution run to the 16 GB V100 ceiling? | 9136 | peak VRAM is a clean signal; negative headroom means it OOMs a V100. |
| mined_quant_sensitivity | What accuracy and VRAM does 4/8/16-bit cost, per doc_type? | 8104 | delta vs the 16-bit baseline of the same model; blank when no baseline cached. |
| mined_oom_frontier | Where do cells OOM by rung, resolution, and pages-fed? | 19710 | from status rows; the empirical 'what fits on a V100' map. |
| mined_abstention_by_doctype | How does abstention on unanswerable questions vary by prompt mode and doc_type? | 1591 | extends the planned hallucination table with the doc_type axis. |

## Defined but blocked / deferred

| table | question | status |
| --- | --- | --- |
| mined_evidence_survival | Given the gold page was / was not retrieved in the top-k, what is the downstream accuracy? | BLOCKED on the H100 G2 inference remainder; separates retrieval failure from reasoning failure. |
| mined_truncation_incidence | Where does the qwen3-embedding retrieval cap still truncate pages, and does it hurt retrieval accuracy? | OPTIONAL/low-value: reads the qwen3-embedding memo rows (seq_len_cap/truncated_pages in retrievers/text.py), not the eval rows. Ship only if truncation is actually nonzero (it should read zero). |
