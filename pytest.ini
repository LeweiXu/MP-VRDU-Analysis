[pytest]
# Collect only the active v4 suite. old/ is the parked v3 snapshot and .cache is a
# local conda package cache with tens of thousands of .py files; neither should be
# walked.
testpaths = tests
norecursedirs = old .cache .data envs results logs .git docs
