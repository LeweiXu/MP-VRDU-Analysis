# Stage 0 declaration only. Stage 1 validates these pins against Kaya's live
# CUDA/module stack before any full experiment runs.
torch==2.7.0
vllm==0.9.2
transformers==4.57.6
accelerate==1.8.1
qwen-vl-utils==0.0.11
PyMuPDF==1.26.3
paddleocr==3.1.0
paddlex[ie,multimodal,ocr]>=3.1.0,<3.2.0
paddlepaddle==3.0.0
docling==2.41.0
marker-pdf==1.10.2
rank-bm25==0.2.2
FlagEmbedding==1.3.5
colpali-engine==0.3.13
# Quantized reasoner (4-bit/8-bit) so the 8B fits a single 16GB V100
# (config.quantization / --quantization). See docs AGENT_GUIDE + SINGLE_GPU_8B_FEASIBILITY.md.
bitsandbytes==0.49.2
# Judge backends (phase-2, login-node/online). Both are already transitive deps
# (openai via vllm/marker, google-genai via marker); pinned here to be explicit.
# openai is capped at 1.90.0 by vllm 0.9.2 (openai<=1.90.0) - do not raise it.
openai==1.90.0
google-genai==1.75.0
datasets==3.6.0
huggingface_hub==0.34.4
hf_xet>=1.1.5,<2.0.0
pillow==10.4.0
numpy==2.2.6
scipy==1.15.3
pandas==2.3.1
requests==2.32.4
chardet==5.2.0
pytest==8.4.1
