"""Operational entry points and cluster tooling.

Holds the three role entry points (generate, judge, build), the Kaya SLURM
runner, YAML run specs, and standalone utility scripts. Kept out of the science
spine so the core pipeline stays root-relative.
"""
