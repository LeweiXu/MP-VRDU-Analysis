# Sub-study A (retrieval method) — BM25, image generation block.
# Fix representation (PyMuPDF4LLM, page) + generation modality (image); vary the
# retriever across the subA configs. Sweep top_k per condition.
name: subA-bm25-image
seed: 0
data: {name: mmlongbench-doc, slice: full}
representation: {parser: pymupdf4llm, chunking: page, dpi: 144}
retrieval: {method: bm25, top_k: 4}
generation:
  modality: image
  generator: kaya_vlm
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
judge: {type: rule}
